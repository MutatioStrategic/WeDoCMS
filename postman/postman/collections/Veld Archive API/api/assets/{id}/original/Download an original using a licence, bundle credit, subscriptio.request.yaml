$kind: http-request
name: Download an original using a licence, bundle credit, subscription, owner, staff, or introductory allowance entitlement
url: "{{baseUrl}}/api/assets/:id/original"
method: GET
pathVariables:
  id: string
auth:
  type: apikey
  credentials:
    key: va_session
    value: "{{apiKey}}"
    in: header
settings: {}
examples: ./.resources/Download an original using a licence, bundle credit, subscriptio.resources/examples
order: 1000
