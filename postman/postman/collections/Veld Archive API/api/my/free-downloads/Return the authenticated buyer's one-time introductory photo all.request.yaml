$kind: http-request
name: Return the authenticated buyer's one-time introductory photo allowance
url: "{{baseUrl}}/api/my/free-downloads"
method: GET
auth:
  type: apikey
  credentials:
    key: va_session
    value: "{{apiKey}}"
    in: header
settings: {}
examples: ./.resources/Return the authenticated buyer's one-time introductory photo all.resources/examples
order: 1000
