$kind: http-request
name: Persist the authenticated seller profile and versioned seller agreement acceptance
url: "{{baseUrl}}/api/onboarding/seller"
method: PUT
headers:
  Authorization: "VeldSession {{sessionToken}}"
  X-CSRF-Token: "{{csrfToken}}"
settings: {}
examples: ./.resources/Persist the authenticated seller profile and versioned seller ag.resources/examples
order: 1000
