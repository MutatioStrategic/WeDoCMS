$kind: http-request
name: Create a hosted Didit KYC or KYB session for the authenticated seller
url: "{{baseUrl}}/api/onboarding/didit/session"
method: POST
auth:
  type: apikey
  credentials:
    key: va_session
    value: "{{apiKey}}"
    in: header
settings: {}
examples: ./.resources/Create a hosted Didit KYC or KYB session for the authenticated s.resources/examples
order: 1000
