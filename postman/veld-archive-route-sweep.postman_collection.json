{
  "info": {
    "name": "Veld Archive — endpoint test plan",
    "_postman_id": "veld-archive-route-sweep",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "description": "Generated from the API route declarations in src/worker/index.ts. Start with the setup folder, select a demoRole, and run the collection. The export mode omits the external Supabase signup boundary, skips data-changing and external requests by default, and leaves credential-like variables blank."
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "pm.collectionVariables.set('routeCount', '178');",
          "if (!pm.collectionVariables.get('testEmail')) pm.collectionVariables.set('testEmail', 'postman-smoke-' + Date.now() + '@example.com');"
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "https://veld-archive-api-demo.blewisorlando.workers.dev"
    },
    {
      "key": "desktopUrl",
      "value": "https://veld-archive.pages.dev"
    },
    {
      "key": "supabaseUrl",
      "value": "https://fmqwymgmrkrnfkugogbw.supabase.co"
    },
    {
      "key": "supabasePublishableKey",
      "value": ""
    },
    {
      "key": "testEmail",
      "value": ""
    },
    {
      "key": "testPassword",
      "value": ""
    },
    {
      "key": "demoRole",
      "value": "contributor",
      "description": "Set to buyer, contributor, editor, or admin before running the collection."
    },
    {
      "key": "runDemoAuth",
      "value": "true",
      "description": "Enable only for the isolated demo or local Worker. Production must use real controlled identities instead."
    },
    {
      "key": "runWrites",
      "value": "false",
      "description": "Set to true only when the target is an isolated demo/test database. Controls data-changing requests."
    },
    {
      "key": "runExternalWrites",
      "value": "false",
      "description": "Set to true only with safe provider credentials/signatures. Controls payment, webhook, integration, and security-boundary requests."
    },
    {
      "key": "maxResponseTimeMs",
      "value": "20000",
      "description": "Maximum response time used by every endpoint test."
    },
    {
      "key": "identityToken",
      "value": "",
      "description": "Optional verified external identity token for the identity-exchange request."
    },
    {
      "key": "sessionToken",
      "value": ""
    },
    {
      "key": "csrfToken",
      "value": ""
    }
  ],
  "item": [
    {
      "name": "00 - Start here",
      "description": "Run the demo session request first. Change demoRole and rerun the setup request when switching buyer, contributor (seller-facing), editor, or admin coverage.",
      "item": [
        {
          "name": "Start demo session for {{demoRole}}",
          "description": "Run this first. Change demoRole at collection level to buyer, contributor, editor, or admin and rerun this request before testing that role's endpoints.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\"role\":\"{{demoRole}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/demo-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "demo-login"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.variables.get('runDemoAuth') !== 'true') {",
                  "  console.warn('Skipped demo login. Set runDemoAuth=true only for an isolated demo or local Worker.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('demo login succeeds', () => pm.expect(pm.response.code).to.eql(200));",
                  "const body = pm.response.json();",
                  "pm.test('selected demo role is active', () => pm.expect(body.user?.role).to.eql(pm.collectionVariables.get('demoRole')));",
                  "pm.test('CSRF token is available for mutations', () => pm.expect(body.csrfToken).to.be.a('string').and.not.empty);",
                  "if (body.csrfToken) pm.collectionVariables.set('csrfToken', body.csrfToken);"
                ]
              }
            }
          ]
        },
        {
          "name": "Verify selected demo session",
          "description": "Confirms that Postman's cookie jar retained the session created by the setup request.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/session",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "session"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.variables.get('runDemoAuth') !== 'true') {",
                  "  console.warn('Skipped demo session verification. Set runDemoAuth=true only for an isolated demo or local Worker.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('session endpoint succeeds', () => pm.expect(pm.response.code).to.eql(200));",
                  "const body = pm.response.json();",
                  "pm.test('demo session is authenticated', () => pm.expect(body.authenticated).to.eql(true));",
                  "pm.test('session role matches demoRole', () => pm.expect(body.user?.role).to.eql(pm.collectionVariables.get('demoRole')));",
                  "if (body.csrfToken) pm.collectionVariables.set('csrfToken', body.csrfToken);"
                ]
              }
            }
          ]
        },
        {
          "name": "Desktop web shell",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "text/html"
              }
            ],
            "url": {
              "raw": "{{desktopUrl}}/",
              "host": [
                "{{desktopUrl}}"
              ],
              "path": []
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('desktop web shell is available', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('desktop web shell returns HTML', () => pm.expect(pm.response.headers.get('content-type') || '').to.include('text/html'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "00 - Optional identity exchange",
      "description": "Set identityToken and runExternalWrites=true only when a verified external JWT is available.",
      "item": [
        {
          "name": "Veld identity exchange",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{identityToken}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\"sessionTransport\":\"bearer\",\"accountIntent\":\"seller\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/exchange",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "exchange"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true' || !pm.collectionVariables.get('identityToken')) {",
                  "  console.warn('Skipped identity exchange. Set identityToken and runExternalWrites=true only for a controlled test.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('identity exchange returns a non-error response', () => pm.expect(pm.response.code).to.be.below(300));",
                  "const body = pm.response.json();",
                  "if (body.sessionToken) pm.collectionVariables.set('sessionToken', body.sessionToken);",
                  "if (body.csrfToken) pm.collectionVariables.set('csrfToken', body.csrfToken);"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "02 - Catalogue, search and public information",
      "description": "Generated endpoint coverage for 02 - catalogue, search and public information. Every request carries route, latency, server-error, JSON, and client-error test points.",
      "item": [
        {
          "name": "GET /api/legal/agreements",
          "description": "Route: GET /api/legal/agreements\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/legal/agreements",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "legal",
                "agreements"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/legal/agreements\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/assets",
          "description": "Route: POST /api/assets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/assets\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /api/assets/:id",
          "description": "Route: PATCH /api/assets/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"PATCH /api/assets/:id\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/assets/:id/versions",
          "description": "Route: GET /api/assets/:id/versions\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain/versions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain",
                "versions"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/assets/:id/versions\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/health",
          "description": "Route: GET /api/health\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/health",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "health"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/health\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/assets/:id/preview-access",
          "description": "Route: GET /api/assets/:id/preview-access\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain/preview-access",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain",
                "preview-access"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/assets/:id/preview-access\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/search/visual",
          "description": "Route: POST /api/search/visual\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode and only changes the demo session or performs a read-only validation.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/search/visual",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "search",
                "visual"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"query\":\"South Africa\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/search/visual\";",
                  "const runMode = \"session\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/assets",
          "description": "Route: GET /api/assets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/assets\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/assets/facets",
          "description": "Route: GET /api/assets/facets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/facets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "facets"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/assets/facets\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/assets/:id",
          "description": "Route: GET /api/assets/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/assets/:id\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/assets/:id/edit-versions",
          "description": "Route: GET /api/assets/:id/edit-versions\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain/edit-versions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain",
                "edit-versions"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/assets/:id/edit-versions\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/assets/:id/edit-versions",
          "description": "Route: POST /api/assets/:id/edit-versions\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain/edit-versions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain",
                "edit-versions"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/assets/:id/edit-versions\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/assets/:id/derivatives",
          "description": "Route: POST /api/assets/:id/derivatives\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain/derivatives",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain",
                "derivatives"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/assets/:id/derivatives\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/discovery",
          "description": "Route: GET /api/discovery\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/discovery",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "discovery"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/discovery\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "01 - Identity and session",
      "description": "Generated endpoint coverage for 01 - identity and session. Every request carries route, latency, server-error, JSON, and client-error test points.",
      "item": [
        {
          "name": "GET /api/auth/session",
          "description": "Route: GET /api/auth/session\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/session",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "session"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/auth/session\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));",
                  "if (status < 300 && contentType.includes('application/json')) { const body = pm.response.json(); if (body.sessionToken) pm.collectionVariables.set('sessionToken', body.sessionToken); if (body.csrfToken) pm.collectionVariables.set('csrfToken', body.csrfToken); }"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/auth/config",
          "description": "Route: GET /api/auth/config\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/config",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "config"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/auth/config\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/auth/logout",
          "description": "Route: POST /api/auth/logout\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode and only changes the demo session or performs a read-only validation.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/logout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "logout"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/auth/logout\";",
                  "const runMode = \"session\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/auth/dev-login",
          "description": "Route: POST /api/auth/dev-login\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode and only changes the demo session or performs a read-only validation.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/dev-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "dev-login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"role\":\"{{demoRole}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.variables.get('runDemoAuth') !== 'true') {",
                  "  console.warn('Skipped demo authentication on this target. Set runDemoAuth=true only for an isolated demo or local Worker.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/auth/dev-login\";",
                  "const runMode = \"session\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));",
                  "if (status < 300 && contentType.includes('application/json')) { const body = pm.response.json(); if (body.sessionToken) pm.collectionVariables.set('sessionToken', body.sessionToken); if (body.csrfToken) pm.collectionVariables.set('csrfToken', body.csrfToken); }"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/auth/demo-login",
          "description": "Route: POST /api/auth/demo-login\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode and only changes the demo session or performs a read-only validation.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/demo-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "demo-login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"role\":\"{{demoRole}}\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.variables.get('runDemoAuth') !== 'true') {",
                  "  console.warn('Skipped demo authentication on this target. Set runDemoAuth=true only for an isolated demo or local Worker.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/auth/demo-login\";",
                  "const runMode = \"session\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));",
                  "if (status < 300 && contentType.includes('application/json')) { const body = pm.response.json(); if (body.sessionToken) pm.collectionVariables.set('sessionToken', body.sessionToken); if (body.csrfToken) pm.collectionVariables.set('csrfToken', body.csrfToken); }"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/auth/exchange",
          "description": "Route: POST /api/auth/exchange\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/exchange",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "exchange"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"sessionTransport\":\"bearer\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/auth/exchange\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/me",
          "description": "Route: GET /api/me\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/me\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));",
                  "if (status < 300 && contentType.includes('application/json')) { const body = pm.response.json(); if (body.sessionToken) pm.collectionVariables.set('sessionToken', body.sessionToken); if (body.csrfToken) pm.collectionVariables.set('csrfToken', body.csrfToken); }"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/notifications",
          "description": "Route: GET /api/notifications\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/notifications",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notifications"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/notifications\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/notifications/:id/read",
          "description": "Route: POST /api/notifications/:id/read\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/notifications/asset-table-mountain/read",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "notifications",
                "asset-table-mountain",
                "read"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/notifications/:id/read\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/organization/members",
          "description": "Route: GET /api/organization/members\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/organization/members",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "organization",
                "members"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/organization/members\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/organization/invitations",
          "description": "Route: POST /api/organization/invitations\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/organization/invitations",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "organization",
                "invitations"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/organization/invitations\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/organization/invitations/accept",
          "description": "Route: POST /api/organization/invitations/accept\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/organization/invitations/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "organization",
                "invitations",
                "accept"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/organization/invitations/accept\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/auth/switch-organization",
          "description": "Route: POST /api/auth/switch-organization\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode and only changes the demo session or performs a read-only validation.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/switch-organization",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "switch-organization"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/auth/switch-organization\";",
                  "const runMode = \"session\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "07 - Integrations, security and webhooks",
      "description": "Generated endpoint coverage for 07 - integrations, security and webhooks. Every request carries route, latency, server-error, JSON, and client-error test points.",
      "item": [
        {
          "name": "POST /api/integrations/wordpress/pairing",
          "description": "Route: POST /api/integrations/wordpress/pairing\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/wordpress/pairing",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "wordpress",
                "pairing"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/integrations/wordpress/pairing\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/integrations/wordpress/pairing/exchange",
          "description": "Route: POST /api/integrations/wordpress/pairing/exchange\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/wordpress/pairing/exchange",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "wordpress",
                "pairing",
                "exchange"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/integrations/wordpress/pairing/exchange\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/integrations/wordpress/connections/:id/revoke",
          "description": "Route: POST /api/integrations/wordpress/connections/:id/revoke\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/wordpress/connections/asset-table-mountain/revoke",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "wordpress",
                "connections",
                "asset-table-mountain",
                "revoke"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/integrations/wordpress/connections/:id/revoke\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/integrations/wordpress/v1/me",
          "description": "Route: GET /api/integrations/wordpress/v1/me\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/wordpress/v1/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "wordpress",
                "v1",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/integrations/wordpress/v1/me\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/integrations/wordpress/v1/assets",
          "description": "Route: GET /api/integrations/wordpress/v1/assets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/wordpress/v1/assets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "wordpress",
                "v1",
                "assets"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/integrations/wordpress/v1/assets\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/webhooks/payments",
          "description": "Route: POST /api/webhooks/payments\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/webhooks/payments",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "webhooks",
                "payments"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/webhooks/payments\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/webhooks/payfast",
          "description": "Route: POST /api/webhooks/payfast\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/webhooks/payfast",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "webhooks",
                "payfast"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/webhooks/payfast\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/webhooks/didit",
          "description": "Route: POST /api/webhooks/didit\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/webhooks/didit",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "webhooks",
                "didit"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/webhooks/didit\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/webhooks/kyc",
          "description": "Route: POST /api/webhooks/kyc\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/webhooks/kyc",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "webhooks",
                "kyc"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/webhooks/kyc\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/integrations/zoho/status",
          "description": "Route: GET /api/integrations/zoho/status\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/zoho/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "zoho",
                "status"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/integrations/zoho/status\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/integrations/zoho/connect/start",
          "description": "Route: POST /api/integrations/zoho/connect/start\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/zoho/connect/start",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "zoho",
                "connect",
                "start"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/integrations/zoho/connect/start\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/integrations/zoho/oauth/callback",
          "description": "Route: GET /api/integrations/zoho/oauth/callback\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/zoho/oauth/callback",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "zoho",
                "oauth",
                "callback"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/integrations/zoho/oauth/callback\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/integrations/zoho/crm/validate",
          "description": "Route: POST /api/integrations/zoho/crm/validate\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/zoho/crm/validate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "zoho",
                "crm",
                "validate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/integrations/zoho/crm/validate\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/integrations/zoho/outbox",
          "description": "Route: GET /api/integrations/zoho/outbox\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/zoho/outbox",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "zoho",
                "outbox"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/integrations/zoho/outbox\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/integrations/zoho/outbox/:id/retry",
          "description": "Route: POST /api/integrations/zoho/outbox/:id/retry\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/integrations/zoho/outbox/asset-table-mountain/retry",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "integrations",
                "zoho",
                "outbox",
                "asset-table-mountain",
                "retry"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/integrations/zoho/outbox/:id/retry\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/webhooks/subscriptions",
          "description": "Route: GET /api/webhooks/subscriptions\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/webhooks/subscriptions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "webhooks",
                "subscriptions"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/webhooks/subscriptions\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/webhooks/subscriptions",
          "description": "Route: POST /api/webhooks/subscriptions\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/webhooks/subscriptions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "webhooks",
                "subscriptions"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/webhooks/subscriptions\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /api/webhooks/subscriptions/:id",
          "description": "Route: DELETE /api/webhooks/subscriptions/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/webhooks/subscriptions/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "webhooks",
                "subscriptions",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"DELETE /api/webhooks/subscriptions/:id\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/security/turnstile",
          "description": "Route: POST /api/security/turnstile\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/security/turnstile",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "security",
                "turnstile"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"token\":\"postman-test-token\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/security/turnstile\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/assets/:id/stream-upload",
          "description": "Route: POST /api/assets/:id/stream-upload\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain/stream-upload",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain",
                "stream-upload"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/assets/:id/stream-upload\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/assets/:id/stream-playback",
          "description": "Route: GET /api/assets/:id/stream-playback\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/assets/asset-table-mountain/stream-playback",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "assets",
                "asset-table-mountain",
                "stream-playback"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/assets/:id/stream-playback\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/webhooks/stream",
          "description": "Route: POST /api/webhooks/stream\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/webhooks/stream",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "webhooks",
                "stream"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/webhooks/stream\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "05 - Editorial, governance and administration",
      "description": "Generated endpoint coverage for 05 - editorial, governance and administration. Every request carries route, latency, server-error, JSON, and client-error test points.",
      "item": [
        {
          "name": "GET /api/governance/assets",
          "description": "Route: GET /api/governance/assets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/governance/assets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "governance",
                "assets"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/governance/assets\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/governance/assets/:id/action",
          "description": "Route: POST /api/governance/assets/:id/action\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/governance/assets/asset-table-mountain/action",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "governance",
                "assets",
                "asset-table-mountain",
                "action"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/governance/assets/:id/action\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/onboarding/tenders",
          "description": "Route: GET /api/admin/onboarding/tenders\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/onboarding/tenders",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "onboarding",
                "tenders"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/onboarding/tenders\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/onboarding/wallets/:walletId/verify",
          "description": "Route: POST /api/admin/onboarding/wallets/:walletId/verify\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/onboarding/wallets/postman-missing-wallet/verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "onboarding",
                "wallets",
                "postman-missing-wallet",
                "verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/onboarding/wallets/:walletId/verify\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/onboarding/tenders/:id/decision",
          "description": "Route: POST /api/admin/onboarding/tenders/:id/decision\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/onboarding/tenders/asset-table-mountain/decision",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "onboarding",
                "tenders",
                "asset-table-mountain",
                "decision"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/onboarding/tenders/:id/decision\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/review",
          "description": "Route: GET /api/admin/review\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/review",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "review"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/review\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/assets/:id/review",
          "description": "Route: POST /api/admin/assets/:id/review\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/assets/asset-table-mountain/review",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "assets",
                "asset-table-mountain",
                "review"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/assets/:id/review\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/licence-approvals",
          "description": "Route: GET /api/admin/licence-approvals\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/licence-approvals",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "licence-approvals"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/licence-approvals\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/ops/reconciliation/payments",
          "description": "Route: GET /api/ops/reconciliation/payments\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/ops/reconciliation/payments",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "ops",
                "reconciliation",
                "payments"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/ops/reconciliation/payments\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/payout-batches",
          "description": "Route: POST /api/admin/payout-batches\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/payout-batches",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "payout-batches"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/payout-batches\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/payout-batches/:id/decision",
          "description": "Route: POST /api/admin/payout-batches/:id/decision\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/payout-batches/asset-table-mountain/decision",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "payout-batches",
                "asset-table-mountain",
                "decision"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/payout-batches/:id/decision\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/payout-batches",
          "description": "Route: GET /api/admin/payout-batches\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/payout-batches",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "payout-batches"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/payout-batches\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/payout-batches/:id",
          "description": "Route: GET /api/admin/payout-batches/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/payout-batches/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "payout-batches",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/payout-batches/:id\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/approval-ledger",
          "description": "Route: GET /api/admin/approval-ledger\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/approval-ledger",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "approval-ledger"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/approval-ledger\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/analytics/audit-search",
          "description": "Route: GET /api/admin/analytics/audit-search\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/analytics/audit-search",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "analytics",
                "audit-search"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/analytics/audit-search\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/audit/events",
          "description": "Route: POST /api/audit/events\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/audit/events",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "audit",
                "events"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/audit/events\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/audit/events/:streamId",
          "description": "Route: GET /api/audit/events/:streamId\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/audit/events/postman-missing-stream",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "audit",
                "events",
                "postman-missing-stream"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/audit/events/:streamId\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/audit/exports",
          "description": "Route: POST /api/audit/exports\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/audit/exports",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "audit",
                "exports"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/audit/exports\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/audit/exports/:exportId",
          "description": "Route: GET /api/audit/exports/:exportId\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/audit/exports/postman-missing-export",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "audit",
                "exports",
                "postman-missing-export"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/audit/exports/:exportId\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/verification/cases",
          "description": "Route: POST /api/verification/cases\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/verification/cases",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "verification",
                "cases"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/verification/cases\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/verification/cases/:caseId",
          "description": "Route: GET /api/verification/cases/:caseId\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/verification/cases/postman-missing-case",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "verification",
                "cases",
                "postman-missing-case"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/verification/cases/:caseId\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/verification/cases/:caseId/documents",
          "description": "Route: POST /api/verification/cases/:caseId/documents\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/verification/cases/postman-missing-case/documents",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "verification",
                "cases",
                "postman-missing-case",
                "documents"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/verification/cases/:caseId/documents\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /api/verification/documents/:documentId/content",
          "description": "Route: PUT /api/verification/documents/:documentId/content\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/verification/documents/postman-missing-document/content",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "verification",
                "documents",
                "postman-missing-document",
                "content"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"PUT /api/verification/documents/:documentId/content\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/verification/documents/:documentId/ocr",
          "description": "Route: POST /api/verification/documents/:documentId/ocr\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/verification/documents/postman-missing-document/ocr",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "verification",
                "documents",
                "postman-missing-document",
                "ocr"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/verification/documents/:documentId/ocr\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/ops/readiness",
          "description": "Route: GET /api/ops/readiness\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/ops/readiness",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "ops",
                "readiness"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/ops/readiness\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/launch-readiness",
          "description": "Route: GET /api/admin/launch-readiness\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/launch-readiness",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "launch-readiness"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/launch-readiness\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/photo-index/rebuild",
          "description": "Route: POST /api/admin/photo-index/rebuild\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/photo-index/rebuild",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "photo-index",
                "rebuild"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/photo-index/rebuild\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/media/previews/rebuild",
          "description": "Route: POST /api/admin/media/previews/rebuild\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/media/previews/rebuild",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "media",
                "previews",
                "rebuild"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/media/previews/rebuild\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/media/integrity",
          "description": "Route: GET /api/admin/media/integrity\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/media/integrity",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "media",
                "integrity"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/media/integrity\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/media/reconcile",
          "description": "Route: POST /api/admin/media/reconcile\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/media/reconcile",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "media",
                "reconcile"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/media/reconcile\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/photo-jobs",
          "description": "Route: GET /api/admin/photo-jobs\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/photo-jobs",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "photo-jobs"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/photo-jobs\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/photo-jobs/:jobId/provenance",
          "description": "Route: GET /api/admin/photo-jobs/:jobId/provenance\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/photo-jobs/postman-missing-job/provenance",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "photo-jobs",
                "postman-missing-job",
                "provenance"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/photo-jobs/:jobId/provenance\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/photo-jobs/:jobId/replay",
          "description": "Route: POST /api/admin/photo-jobs/:jobId/replay\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/photo-jobs/postman-missing-job/replay",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "photo-jobs",
                "postman-missing-job",
                "replay"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/photo-jobs/:jobId/replay\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/photo-jobs/:jobId/re-enrich",
          "description": "Route: POST /api/admin/photo-jobs/:jobId/re-enrich\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/photo-jobs/postman-missing-job/re-enrich",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "photo-jobs",
                "postman-missing-job",
                "re-enrich"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/photo-jobs/:jobId/re-enrich\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/search-insights",
          "description": "Route: GET /api/admin/search-insights\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/search-insights",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "search-insights"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/search-insights\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/admin/community/curation",
          "description": "Route: GET /api/admin/community/curation\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/community/curation",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "community",
                "curation"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/admin/community/curation\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/community/showcases",
          "description": "Route: POST /api/admin/community/showcases\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/community/showcases",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "community",
                "showcases"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/community/showcases\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /api/admin/community/showcases/:id",
          "description": "Route: PATCH /api/admin/community/showcases/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/community/showcases/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "community",
                "showcases",
                "asset-table-mountain"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"PATCH /api/admin/community/showcases/:id\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /api/admin/community/showcases/:id",
          "description": "Route: DELETE /api/admin/community/showcases/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/community/showcases/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "community",
                "showcases",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"DELETE /api/admin/community/showcases/:id\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/community/collections",
          "description": "Route: POST /api/admin/community/collections\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/community/collections",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "community",
                "collections"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/community/collections\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /api/admin/community/collections/:id",
          "description": "Route: PATCH /api/admin/community/collections/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/community/collections/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "community",
                "collections",
                "asset-table-mountain"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"PATCH /api/admin/community/collections/:id\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /api/admin/community/collections/:id",
          "description": "Route: DELETE /api/admin/community/collections/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/community/collections/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "community",
                "collections",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"DELETE /api/admin/community/collections/:id\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/admin/community/posts/:postId/moderate",
          "description": "Route: POST /api/admin/community/posts/:postId/moderate\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/community/posts/postman-missing-post/moderate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "community",
                "posts",
                "postman-missing-post",
                "moderate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/admin/community/posts/:postId/moderate\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "04 - Contributor and media workflow",
      "description": "Generated endpoint coverage for 04 - contributor and media workflow. Every request carries route, latency, server-error, JSON, and client-error test points.",
      "item": [
        {
          "name": "GET /api/onboarding",
          "description": "Route: GET /api/onboarding\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/onboarding",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "onboarding"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/onboarding\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /api/onboarding",
          "description": "Route: PUT /api/onboarding\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/onboarding",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "onboarding"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"PUT /api/onboarding\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/onboarding/status",
          "description": "Route: GET /api/onboarding/status\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/onboarding/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "onboarding",
                "status"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/onboarding/status\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/onboarding/wallet",
          "description": "Route: POST /api/onboarding/wallet\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/onboarding/wallet",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "onboarding",
                "wallet"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/onboarding/wallet\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/onboarding/contract",
          "description": "Route: POST /api/onboarding/contract\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/onboarding/contract",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "onboarding",
                "contract"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/onboarding/contract\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/my/assets",
          "description": "Route: GET /api/my/assets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/my/assets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "my",
                "assets"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/my/assets\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /api/onboarding/seller",
          "description": "Route: PUT /api/onboarding/seller\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/onboarding/seller",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "onboarding",
                "seller"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"PUT /api/onboarding/seller\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/onboarding/cipc/lookup",
          "description": "Route: POST /api/onboarding/cipc/lookup\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/onboarding/cipc/lookup",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "onboarding",
                "cipc",
                "lookup"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/onboarding/cipc/lookup\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/onboarding/didit/session",
          "description": "Route: POST /api/onboarding/didit/session\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/onboarding/didit/session",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "onboarding",
                "didit",
                "session"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/onboarding/didit/session\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/creators",
          "description": "Route: GET /api/creators\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/creators",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "creators"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/creators\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/creators/:slug",
          "description": "Route: GET /api/creators/:slug\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/creators/table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "creators",
                "table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/creators/:slug\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/analytics/contributor",
          "description": "Route: GET /api/analytics/contributor\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/analytics/contributor",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "analytics",
                "contributor"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/analytics/contributor\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/analytics/contributor/assets/:id",
          "description": "Route: GET /api/analytics/contributor/assets/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/analytics/contributor/assets/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "analytics",
                "contributor",
                "assets",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/analytics/contributor/assets/:id\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/analytics/contributor/revenue",
          "description": "Route: GET /api/analytics/contributor/revenue\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/analytics/contributor/revenue",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "analytics",
                "contributor",
                "revenue"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/analytics/contributor/revenue\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/analytics/contributor/revenue.csv",
          "description": "Route: GET /api/analytics/contributor/revenue.csv\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/analytics/contributor/revenue.csv",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "analytics",
                "contributor",
                "revenue.csv"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/analytics/contributor/revenue.csv\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/analytics/contributor/revenue.pdf",
          "description": "Route: GET /api/analytics/contributor/revenue.pdf\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/analytics/contributor/revenue.pdf",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "analytics",
                "contributor",
                "revenue.pdf"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/analytics/contributor/revenue.pdf\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/uploads",
          "description": "Route: POST /api/uploads\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/uploads",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "uploads"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/uploads\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/uploads/:uploadId/complete",
          "description": "Route: POST /api/uploads/:uploadId/complete\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/uploads/postman-missing-upload/complete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "uploads",
                "postman-missing-upload",
                "complete"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/uploads/:uploadId/complete\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "03 - Buyer, licensing and payments",
      "description": "Generated endpoint coverage for 03 - buyer, licensing and payments. Every request carries route, latency, server-error, JSON, and client-error test points.",
      "item": [
        {
          "name": "POST /api/checkout/validate",
          "description": "Route: POST /api/checkout/validate\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode and only changes the demo session or performs a read-only validation.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/checkout/validate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "checkout",
                "validate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"assetId\":\"asset-table-mountain\",\"licenceProduct\":\"editorial\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/checkout/validate\";",
                  "const runMode = \"session\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/buyer/licence-auto-approval",
          "description": "Route: GET /api/buyer/licence-auto-approval\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/buyer/licence-auto-approval",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "buyer",
                "licence-auto-approval"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/buyer/licence-auto-approval\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /api/buyer/licence-auto-approval",
          "description": "Route: PUT /api/buyer/licence-auto-approval\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/buyer/licence-auto-approval",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "buyer",
                "licence-auto-approval"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"PUT /api/buyer/licence-auto-approval\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/checkout",
          "description": "Route: POST /api/checkout\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/checkout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "checkout"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"assetId\":\"asset-table-mountain\",\"licenceProduct\":\"editorial\"}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/checkout\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/payments/:licenceId/session",
          "description": "Route: POST /api/payments/:licenceId/session\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/payments/postman-missing-licence/session",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "payments",
                "postman-missing-licence",
                "session"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/payments/:licenceId/session\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/payments/:licenceId/settled",
          "description": "Route: POST /api/payments/:licenceId/settled\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/payments/postman-missing-licence/settled",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "payments",
                "postman-missing-licence",
                "settled"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/payments/:licenceId/settled\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/my/licences",
          "description": "Route: GET /api/my/licences\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/my/licences",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "my",
                "licences"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/my/licences\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/subscriptions/checkout",
          "description": "Route: POST /api/subscriptions/checkout\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/subscriptions/checkout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "subscriptions",
                "checkout"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/subscriptions/checkout\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/subscriptions/:id",
          "description": "Route: GET /api/subscriptions/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/subscriptions/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "subscriptions",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/subscriptions/:id\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/subscriptions/:id/cancel",
          "description": "Route: POST /api/subscriptions/:id/cancel\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/subscriptions/asset-table-mountain/cancel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "subscriptions",
                "asset-table-mountain",
                "cancel"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/subscriptions/:id/cancel\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/subscription",
          "description": "Route: GET /api/subscription\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/subscription",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "subscription"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/subscription\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/subscription/session",
          "description": "Route: POST /api/subscription/session\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/subscription/session",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "subscription",
                "session"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/subscription/session\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/subscription/manage-link",
          "description": "Route: POST /api/subscription/manage-link\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runExternalWrites is set to true; it can call an external provider, webhook, payment, or security boundary.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/subscription/manage-link",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "subscription",
                "manage-link"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runExternalWrites') !== 'true') {",
                  "  console.warn('Skipped external-boundary request. Set collection variable runExternalWrites=true only with safe test credentials and signatures.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/subscription/manage-link\";",
                  "const runMode = \"external\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/buyer/platform-subscription/checkout",
          "description": "Route: POST /api/buyer/platform-subscription/checkout\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/buyer/platform-subscription/checkout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "buyer",
                "platform-subscription",
                "checkout"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/buyer/platform-subscription/checkout\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/buyer/credits/checkout",
          "description": "Route: POST /api/buyer/credits/checkout\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/buyer/credits/checkout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "buyer",
                "credits",
                "checkout"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/buyer/credits/checkout\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/my/purchases",
          "description": "Route: GET /api/my/purchases\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/my/purchases",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "my",
                "purchases"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/my/purchases\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/my/free-downloads",
          "description": "Route: GET /api/my/free-downloads\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/my/free-downloads",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "my",
                "free-downloads"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/my/free-downloads\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/licence-products",
          "description": "Route: GET /api/licence-products\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/licence-products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "licence-products"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/licence-products\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/analytics/buyer",
          "description": "Route: GET /api/analytics/buyer\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/analytics/buyer",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "analytics",
                "buyer"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/analytics/buyer\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/lightboxes",
          "description": "Route: GET /api/lightboxes\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/lightboxes",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "lightboxes"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/lightboxes\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/lightboxes",
          "description": "Route: POST /api/lightboxes\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/lightboxes",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "lightboxes"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/lightboxes\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/lightboxes/shared/:token",
          "description": "Route: GET /api/lightboxes/shared/:token\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/lightboxes/shared/postman-missing-token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "lightboxes",
                "shared",
                "postman-missing-token"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/lightboxes/shared/:token\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/lightboxes/:id",
          "description": "Route: GET /api/lightboxes/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/lightboxes/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "lightboxes",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/lightboxes/:id\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/lightboxes/:id/assets",
          "description": "Route: POST /api/lightboxes/:id/assets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/lightboxes/asset-table-mountain/assets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "lightboxes",
                "asset-table-mountain",
                "assets"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/lightboxes/:id/assets\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/lightboxes/:id/share-link",
          "description": "Route: POST /api/lightboxes/:id/share-link\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/lightboxes/asset-table-mountain/share-link",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "lightboxes",
                "asset-table-mountain",
                "share-link"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/lightboxes/:id/share-link\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /api/lightboxes/:id/assets/:assetId",
          "description": "Route: DELETE /api/lightboxes/:id/assets/:assetId\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/lightboxes/asset-table-mountain/assets/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "lightboxes",
                "asset-table-mountain",
                "assets",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"DELETE /api/lightboxes/:id/assets/:assetId\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /api/lightboxes/:id",
          "description": "Route: DELETE /api/lightboxes/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/lightboxes/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "lightboxes",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"DELETE /api/lightboxes/:id\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "08 - Other API coverage",
      "description": "Generated endpoint coverage for 08 - other api coverage. Every request carries route, latency, server-error, JSON, and client-error test points.",
      "item": [
        {
          "name": "GET /api/demo/payments/:licenceId/complete",
          "description": "Route: GET /api/demo/payments/:licenceId/complete\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/demo/payments/postman-missing-licence/complete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "demo",
                "payments",
                "postman-missing-licence",
                "complete"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/demo/payments/:licenceId/complete\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/my/subscriptions",
          "description": "Route: GET /api/my/subscriptions\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/my/subscriptions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "my",
                "subscriptions"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/my/subscriptions\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/account/lifecycle",
          "description": "Route: GET /api/account/lifecycle\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/account/lifecycle",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "account",
                "lifecycle"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/account/lifecycle\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /api/account/preferences",
          "description": "Route: PUT /api/account/preferences\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/account/preferences",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "account",
                "preferences"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"PUT /api/account/preferences\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/account/exports",
          "description": "Route: POST /api/account/exports\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/account/exports",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "account",
                "exports"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/account/exports\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/account/deletion",
          "description": "Route: POST /api/account/deletion\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/account/deletion",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "account",
                "deletion"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/account/deletion\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/my/platform-subscription",
          "description": "Route: GET /api/my/platform-subscription\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/my/platform-subscription",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "my",
                "platform-subscription"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/my/platform-subscription\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/my/platform-subscription/cancel",
          "description": "Route: POST /api/my/platform-subscription/cancel\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/my/platform-subscription/cancel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "my",
                "platform-subscription",
                "cancel"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/my/platform-subscription/cancel\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/my/credits",
          "description": "Route: GET /api/my/credits\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/my/credits",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "my",
                "credits"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/my/credits\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/analytics/events",
          "description": "Route: POST /api/analytics/events\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/analytics/events",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "analytics",
                "events"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"eventType\":\"postman_route_sweep\",\"payload\":{}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/analytics/events\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/buyer-api-keys",
          "description": "Route: POST /api/buyer-api-keys\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/buyer-api-keys",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "buyer-api-keys"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/buyer-api-keys\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/buyer-api-keys",
          "description": "Route: GET /api/buyer-api-keys\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/buyer-api-keys",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "buyer-api-keys"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/buyer-api-keys\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /api/buyer-api-keys/:id",
          "description": "Route: DELETE /api/buyer-api-keys/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/buyer-api-keys/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "buyer-api-keys",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"DELETE /api/buyer-api-keys/:id\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/public/v1/assets",
          "description": "Route: GET /api/public/v1/assets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/public/v1/assets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "public",
                "v1",
                "assets"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/public/v1/assets\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/public/v1/assets/:id",
          "description": "Route: GET /api/public/v1/assets/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/public/v1/assets/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "public",
                "v1",
                "assets",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/public/v1/assets/:id\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/saved-searches",
          "description": "Route: GET /api/saved-searches\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/saved-searches",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "saved-searches"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/saved-searches\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/saved-searches",
          "description": "Route: POST /api/saved-searches\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/saved-searches",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "saved-searches"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/saved-searches\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /api/saved-searches/:id",
          "description": "Route: DELETE /api/saved-searches/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/saved-searches/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "saved-searches",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"DELETE /api/saved-searches/:id\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "06 - Rights, community and campaigns",
      "description": "Generated endpoint coverage for 06 - rights, community and campaigns. Every request carries route, latency, server-error, JSON, and client-error test points.",
      "item": [
        {
          "name": "GET /api/community/overview",
          "description": "Route: GET /api/community/overview\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/community/overview",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "community",
                "overview"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/community/overview\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/community/forums/:forumId/threads",
          "description": "Route: POST /api/community/forums/:forumId/threads\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/community/forums/postman-missing-forum/threads",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "community",
                "forums",
                "postman-missing-forum",
                "threads"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/community/forums/:forumId/threads\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/community/threads/:threadId/posts",
          "description": "Route: POST /api/community/threads/:threadId/posts\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/community/threads/postman-missing-thread/posts",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "community",
                "threads",
                "postman-missing-thread",
                "posts"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/community/threads/:threadId/posts\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/community/threads/:threadId/posts",
          "description": "Route: GET /api/community/threads/:threadId/posts\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/community/threads/postman-missing-thread/posts",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "community",
                "threads",
                "postman-missing-thread",
                "posts"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/community/threads/:threadId/posts\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/rights/takedown",
          "description": "Route: POST /api/rights/takedown\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/rights/takedown",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "rights",
                "takedown"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/rights/takedown\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/rights/cases",
          "description": "Route: GET /api/rights/cases\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/rights/cases",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "rights",
                "cases"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/rights/cases\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/rights/cases/:id/messages",
          "description": "Route: POST /api/rights/cases/:id/messages\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/rights/cases/asset-table-mountain/messages",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "rights",
                "cases",
                "asset-table-mountain",
                "messages"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/rights/cases/:id/messages\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/rights/cases/:id/messages",
          "description": "Route: GET /api/rights/cases/:id/messages\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/rights/cases/asset-table-mountain/messages",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "rights",
                "cases",
                "asset-table-mountain",
                "messages"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/rights/cases/:id/messages\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/rights/cases/:id/transition",
          "description": "Route: POST /api/rights/cases/:id/transition\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/rights/cases/asset-table-mountain/transition",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "rights",
                "cases",
                "asset-table-mountain",
                "transition"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/rights/cases/:id/transition\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/rights/cases/:id/appeal",
          "description": "Route: POST /api/rights/cases/:id/appeal\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/rights/cases/asset-table-mountain/appeal",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "rights",
                "cases",
                "asset-table-mountain",
                "appeal"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/rights/cases/:id/appeal\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/campaigns",
          "description": "Route: GET /api/campaigns\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/campaigns\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/campaigns",
          "description": "Route: POST /api/campaigns\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/campaigns\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/campaigns/:id",
          "description": "Route: GET /api/campaigns/:id\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/campaigns/:id\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/campaigns/:id/terms/accept",
          "description": "Route: POST /api/campaigns/:id/terms/accept\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain/terms/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain",
                "terms",
                "accept"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/campaigns/:id/terms/accept\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/campaigns/:id/assets",
          "description": "Route: POST /api/campaigns/:id/assets\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain/assets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain",
                "assets"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/campaigns/:id/assets\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/campaigns/:id/bundles",
          "description": "Route: POST /api/campaigns/:id/bundles\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain/bundles",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain",
                "bundles"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/campaigns/:id/bundles\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/campaigns/:id/bundles/:bundleId/approve",
          "description": "Route: POST /api/campaigns/:id/bundles/:bundleId/approve\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain/bundles/postman-missing-bundleId/approve",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain",
                "bundles",
                "postman-missing-bundleId",
                "approve"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/campaigns/:id/bundles/:bundleId/approve\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/campaign-bundles/:id/download",
          "description": "Route: GET /api/campaign-bundles/:id/download\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaign-bundles/asset-table-mountain/download",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaign-bundles",
                "asset-table-mountain",
                "download"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/campaign-bundles/:id/download\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /api/campaigns/:id/manifest",
          "description": "Route: GET /api/campaigns/:id/manifest\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Runs in the default safe mode.",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain/manifest",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain",
                "manifest"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"GET /api/campaigns/:id/manifest\";",
                  "const runMode = \"read\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/campaigns/:id/integrations/zoho/crm",
          "description": "Route: POST /api/campaigns/:id/integrations/zoho/crm\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain/integrations/zoho/crm",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain",
                "integrations",
                "zoho",
                "crm"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/campaigns/:id/integrations/zoho/crm\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/campaigns/:id/integrations/zoho/campaigns",
          "description": "Route: POST /api/campaigns/:id/integrations/zoho/campaigns\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain/integrations/zoho/campaigns",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain",
                "integrations",
                "zoho",
                "campaigns"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/campaigns/:id/integrations/zoho/campaigns\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/rights/cases/:id/integrations/zoho/desk",
          "description": "Route: POST /api/rights/cases/:id/integrations/zoho/desk\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/rights/cases/asset-table-mountain/integrations/zoho/desk",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "rights",
                "cases",
                "asset-table-mountain",
                "integrations",
                "zoho",
                "desk"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/rights/cases/:id/integrations/zoho/desk\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /api/campaigns/:id/integrations/zoho/social",
          "description": "Route: POST /api/campaigns/:id/integrations/zoho/social\n\nAutomated test points:\n- The API returns an HTTP response within the configured response-time budget.\n- The route does not return an unhandled 500, 502, or 504 response.\n- JSON responses parse successfully; JSON 4xx responses provide an error or recovery message.\n- Demo session responses refresh the CSRF token for follow-on mutations.\n\nSafety gate: Skipped unless runWrites is set to true; it can create, update, delete, download an entitlement, or otherwise change demo data.",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "VeldSession {{sessionToken}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrfToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/campaigns/asset-table-mountain/integrations/zoho/social",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "campaigns",
                "asset-table-mountain",
                "integrations",
                "zoho",
                "social"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "if (pm.collectionVariables.get('runWrites') !== 'true') {",
                  "  console.warn('Skipped write request. Set collection variable runWrites=true to execute it.');",
                  "  pm.execution.skipRequest();",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const routeId = \"POST /api/campaigns/:id/integrations/zoho/social\";",
                  "const runMode = \"write\";",
                  "const status = pm.response.code;",
                  "const contentType = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "pm.test(\"178 registered routes are in this collection\", () => pm.expect(pm.collectionVariables.get('routeCount')).to.eql('178'));",
                  "pm.test('API returned an HTTP response', () => pm.expect(status).to.be.within(100, 599));",
                  "pm.test('response completed within the configured budget', () => { const budget = Number(pm.collectionVariables.get('maxResponseTimeMs') || '20000'); pm.expect(pm.response.responseTime).to.be.at.most(budget); });",
                  "pm.test('route has no unhandled server error', () => { if ([500, 502, 504].includes(status)) console.error(JSON.stringify({ route: routeId, status, body: pm.response.text().slice(0, 500) })); pm.expect([500, 502, 504], routeId).to.not.include(status); });",
                  "if (status === 503) console.warn(JSON.stringify({ route: routeId, status, note: 'The route is reachable but a configured dependency is unavailable.' }));",
                  "if (contentType.includes('application/json') && pm.response.text().trim()) {",
                  "  pm.test('JSON response parses', () => pm.response.to.have.jsonBody());",
                  "  if (status >= 400 && status < 500) { const body = pm.response.json(); pm.test('client error explains the next step', () => pm.expect(Boolean(body.error || body.message || body.errors), routeId).to.eql(true)); }",
                  "}",
                  "console.info(JSON.stringify({ route: routeId, status, responseTimeMs: pm.response.responseTime, runMode }));"
                ]
              }
            }
          ]
        }
      ]
    }
  ]
}
